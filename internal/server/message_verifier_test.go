package server

import (
	"testing"
	"github.com/rcsrn/moonchat/cmd/message"
)

func toMap(message message.Message) (map[string]string) {
	return nil
}

func testVerifyIdentifyMessage(t *testing.T) {
	
}
